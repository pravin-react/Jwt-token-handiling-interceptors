export interface loginType {
    username:string,
    password:string
}

export interface regType {
    name: string,
    email: string,
    password: string,
    newpassword: string,
  };

export interface keyValue {
    key: string,
    value: string
}